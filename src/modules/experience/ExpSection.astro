---
import { EXPERIENCE_DATA } from "../../data/experiences";
import ExpItem from "./ExpItem.astro";
---

<div class="grid gap-4">
  <ul class="flex flex-col gap-2">
    {EXPERIENCE_DATA.map((exp, index) => <ExpItem {...exp} />)}
  </ul>
</div>
