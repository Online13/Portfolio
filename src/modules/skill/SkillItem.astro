---
import Card from "../../components/Card.astro";

const { category, skills } = Astro.props;
---

<Card class="group">
  <h3 class="text-sm text-stone-500 group-hover:text-blue-700">{category}</h3>
  <p class="whitespace-nowrap text-sm">{skills.join(", ")}</p>
</Card>
